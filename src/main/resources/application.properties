spring.application.name=dailyplanner
server.port=9200

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/dailyplanner
spring.datasource.username=user
spring.datasource.password=mbeki2019
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true

#http://localhost:8080/api/v1/days/save
#http://localhost:8080/api/v1/days

management.endpoints.web.exposure.include=*
#debug=true
#spring.main.allow-circular-references=true

logging.level.org.springframework.security=TRACE

spring.security.user.name=khumbu
spring.security.user.password=password

#enable setup script
spring.sql.init.mode=always
# Ensure Hibernate creates tables before data.sql runs
spring.jpa.defer-datasource-initialization=true

spring.sql.init.continue-on-error=true